export type Profile = {
    id : string,
    name : string,
    imageURL : string,
    isLocked?: boolean
}

export type Song = {
  id: string,
  link: string,
  image: string,
  title: string,
  description: string
}
